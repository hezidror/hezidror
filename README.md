# דף נחיתה - חזי | עיצוב שיער

דף נחיתה סטטי (HTML/CSS/JS) לעסק "חזי - עיצוב שיער", בעיצוב שחור-זהב תואם ללוגו, מוכן לפרסום ב-GitHub Pages ולשיתוף ברשתות.

## מה יש כאן
- `index.html` - מבנה הדף (הירו, שירותים, גלריה, אודות, יצירת קשר)
- `style.css` - העיצוב (פלטת שחור-זהב לפי הלוגו)
- `script.js` - תפריט מובייל ושנה בפוטר
- `assets/logo.jpg` - לוגו הלקוח, מוצג בכותרת העליונה
- `assets/favicon.png` - פוביקון שנחתך מהלוגו
- `assets/og-image.jpg` - תמונת תצוגה מקדימה לשיתוף ברשתות (נחתכה מהלוגו)

## לפני הפרסום - להחליף:
1. **מספר וואטסאפ** - להחליף את `972500000000` בקישורי ה-WhatsApp (בפורמט בינלאומי בלי `+` וללא אפסים מובילים).
3. **מספר טלפון** - להחליף את `+972500000000` בקישורי ה-`tel:`.
4. **קישורי אינסטגרם/פייסבוק** - להחליף את `your_salon` בשם המשתמש האמיתי.
5. **כתובת** - להחליף את "רחוב הדוגמה 1, העיר שלך".
6. **תמונות** - להוסיף תמונות אמיתיות לתיקיית `assets/` ולהחליף את התאים הריקים בגלריה ואת תמונת "אודות" (כרגע רקעים בצבע).
7. **כתובת הדף לשיתוף** - להחליף את `https://your-username.github.io/your-repo/` (מופיע פעמיים ב-`index.html`, בתגיות `og:url`, `og:image` ו-`twitter:image`) בכתובת האמיתית של הדף אחרי הפרסום.

## שיתוף ברשתות חברתיות
בדף כבר מוגדרות תגיות Open Graph ו-Twitter Card, כך שכששולחים את הקישור בוואטסאפ, פייסבוק, אינסטגרם (בביו) או טוויטר/X תופיע תצוגה מקדימה עם כותרת, תיאור ותמונה (`assets/og-image.jpg`). חשוב לעדכן את הכתובות בסעיף 7 לפני השיתוף הראשון, אחרת התצוגה המקדימה תצביע לכתובת שגויה. אפשר לבדוק איך התצוגה נראית בכלים כמו [Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/) לאחר הפרסום.

## פרסום ב-GitHub Pages
1. בהגדרות הריפו: Settings → Pages.
2. תחת "Build and deployment", לבחור Source: **Deploy from a branch**.
3. לבחור את ה-branch (`main` או הענף שבו נמצאים הקבצים) ותיקיית root (`/`).
4. לשמור - הדף יהיה זמין בכתובת `https://<username>.github.io/<repo>/`.

אפשר גם לפתוח את `index.html` ישירות בדפדפן כדי לראות תצוגה מקדימה מקומית.
